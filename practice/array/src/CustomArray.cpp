#include "CustomArray.h"

int
FindPeakElement(const vector<int>& inputArray)
{
	if (inputArray.size() == 0)
	{
		return 0;
	}

	else
	{
		if (inputArray.size() == 1)
		{
			return inputArray[0];
		}

		else
		{
		
		}
	}
}
